# オフライン2人対戦型絵合わせゲーム システム要件定義書

## 1. 概要・目的
本システムは、電車移動中などのインターネット通信が不安定・あるいは圏外の環境（地下鉄やトンネル内）において、サーバーを用意することなく2台の端末（Windows PCおよびAndroid）間でリアルタイムに1対1対戦を行える、ブラウザベースの絵合わせゲーム（ドンジャラ風アプリ）である。

---

## 2. システム要件

### 2.1 動作環境・技術スタック
* 対象プラットフォーム: Windows PC, Android スマートフォン / タブレット
* 非対象プラットフォーム: iOS, iPadOS, macOS
* 推奨ブラウザ: Google Chrome, Microsoft Edge
* アーキテクチャ: 静的HTML + CSS + Plain JavaScript（外部サーバーレス、GitHub Pagesで公開可能）
* 通信方式: WebRTC DataChannel (完全オフライン P2P 通信 / シグナリングコード・QRコード接続)

### 2.2 システム構成・通信役割
1. 親機（ホスト / DataChannel Owner）
   * P2P接続の招待コード（Offer SDP）を発行し、子機からの応答コード（Answer SDP）を受け取って接続を確立する。
   * ゲーム全体のステート（山札のシャッフル・管理、親/子のターン制御、勝敗判定）を一元管理する。
2. 子機（クライアント / Peer）
   * 親機の招待コードを入力し、応答コードを生成して親機に返信・接続を確立する。
   * 親機からのデータ通知を受け取り、画面の描画を更新する。
   * 自ターンの打牌（牌を捨てる）やアクション（リーチ・あがり）の要求を親機へ送信する。

### 2.3 ゲーム機能要件
1. 手札・山札管理
   * 1人あたり手札9枚（配牌）でスタート。
   * 山札からの「ツモ（1枚引く）」および「打牌（1枚捨てる）」の基本交互ルーチン。
2. 対戦同期
   * 互いの捨て牌（河）および残り山札枚数をリアルタイムで同期表示。
   * 相手の手札は裏面表示（枚数のみ同期）。
3. あがり判定
   * 3枚1セット×3組の役構成チェック。
   * 「あがり（ドンジャラ）」ボタン押下による勝敗判定と得点移動。

---

## 3. 段階的開発ステップ（ロードマップ）

いきなり複雑なゲームロジックと通信処理を組み合わせるとデバッグが難しくなるため、以下の2ステップで段階的に開発を進行する。

* Step 1: WebRTC オフライン P2P チャットアプリの作成
  * ブラウザ間でのWebRTC DataChannel（コード入力によるシグナリング）接続検証
  * 文字データ（JSON文字列）の相互送受信と画面反映の実装
  * オフライン環境（電車内想定）での動作・切断再接続の検証
* Step 2: 対戦型絵合わせゲーム（本編）の開発
  * Step 1の通信基盤の上に、手札9枚・山札・ツモ/打牌同期を実装
  * 役判定・あがり宣言・スコア計算ロジックの統合

---

### Step 1: WebRTC オフライン P2P チャットアプリ（プロトタイプ）

#### 目的
WebRTC DataChannelを用いた「サーバーレス・完全オフライン」での2台間データ相互送受信基盤を確立する。

#### 機能要件
* 役割選択: 起動時に「親機として接続コード発行（Host）」か「子機として接続（Guest）」を選択できる。
* 接続・ペアリング:
  * 親機：招待コード（Offer SDP）を自動生成。
  * 子機：招待コードを入力し、応答コード（Answer SDP）を生成。親機が応答コードを入力してダイレクトP2P接続確立。
* メッセージ送受信:
  * 入力エリアに入力したテキストを相手端末に送信し、双方向のチャットログ画面に即座に表示する。
  * 送受信データは将来的なゲームコマンド拡張を見据え、JSON形式（例: {"type": "chat", "message": "こんにちは"}）でパケット化して扱う。
* 接続状態表示: 「未接続」「接続中」「通信エラー」などのステータスを画面上に視覚的に表示する。

---

### Step 2: オフライン対戦型絵合わせゲーム（本編）

#### 目的
Step 1で構築したBluetooth通信ロジックをベースに、対戦型絵合わせゲームの画面（UI）とゲームエンジン（ロジック）を統合する。

#### 機能要件
* ゲームボードUI:
  * 自分の手札（9枚＋ツモ1枚）、相手の捨て牌、自分の捨て牌、山札数を直感的にタップ操作できるUIレイアウト。
* データ同期プロトコル:
  * GAME_START: 配牌データの送信
  * DRAW: 山札から1枚引く
  * DISCARD: 手札から1枚捨てる（相手画面の捨て牌エリアに反映）
  * WIN: あがり宣言と役の確認・照合
* データ保持（localStorage）:
  * 直近の接続情報やプレイヤー名、対戦スコアのローカル保存。