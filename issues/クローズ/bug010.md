# bug010 pvpで相手のツモが見える
ステータス:解決済み
内容: 自分が子機でも画面上部に表示される「あなた(親機) のターン（ツモ: 🐸 蛙）」のテキストにより、相手が何を取ったか見える。親機でも同様に子機のツモが見える。
修正内容: ターンメッセージからツモした牌の表示を削除し、メッセージは「○○ のターン」のみに変更。ツモ牌は各プレイヤーの自分の selfDraw にのみ表示される（相手側へは送信されない）。PvPの同期パケットの msg にもツモ情報は含まれない。v8 で反映 