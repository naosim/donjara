# bug005: ポンした牌が何かわからない。UIでわかるようにしてほしい
ステータス: 解決済み
内容: 
修正内容: メルド（ポンした3枚組）を表向きで表示する領域を追加（game.js + donjara_game.html）。
- 相手ブロックに `oppMeld`、自分ブロックに `selfMeld` を追加。「ポン」タグ付きの枠に3枚を表向き表示
- 相手の裏カード枚数も手札の実枚数に連動（ポン後は5枚に減る）
- PvP子機にも sendSync で `hostMelds` / `guestMelds` / `hostHandCount` を送信して反映
- DOMモックで親機/子機の両方の表示・送信内容を検証済み