# bug001: 最初に牌が配られた直後にソートしてほしい
ステータス:解決済み
内容: 最初に牌が配られた直後にソートしてほしい。
修正内容:配牌直後に自分の手札を柄順（ジョーカーは最後）で表示するよう game.js を修正。
- 親機/AIモード: startRound の配牌後に `sortSelfHandOrder()` で selfOrder を初期化
- PvP子機: renderGuest が局（round）替わりを検出した時点で selfOrder を再ソート
- 以降はツモ・ポンで増えた牌も既存の表示順を保持（`applyDisplayOrder`）