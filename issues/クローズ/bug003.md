# bug003: リーチのボタンが表示されない
ステータス:解決済み
内容: リーチのボタンが表示されない。
修正内容:リーチ機能を実装（game.js）: `isTenpai()` で「8枚手札+あと1枚で9枚あがり成立」を判定し、playTurnのツモ前に人間へ「リーチを宣言/リーチしない」を提示。
対象例（豚×3・兎×3・パンダ×2 = パンダ待ち）はエンジン・進行・UIの3層で検証済み。
確認した画面が古い場合は Ctrl+Shift+R で再読み込みしてください。