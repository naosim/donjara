# ドンジャラ点数管理アプリ 仕様書

## 1. 概要

本アプリは、家族や友人同士でドンジャラをプレイする際に、面倒な点数計算やチップ移動をスマートフォン・タブレット上で手軽に行うためのWebアプリケーションである。
静的HTML/CSS/JavaScriptで構成され、GitHub Pages上にホスティングして利用することを想定する。

## 2. 動作環境・動作要件

* **対応デバイス**: スマートフォン、タブレット、PC（レスポンシブ対応）

* **ホスティング環境**: GitHub Pages（静的ファイル構成）

* **データ保存機能**: Webブラウザの `localStorage` を使用し、リロード時や次回アクセス時も設定および点数状況を保持

## 3. 機能要件

### 3.1 設定機能

* **プレイヤー設定**

  * プレイヤー人数の設定（2人〜6人対応）

  * 各プレイヤーのニックネーム登録

* **初期点数設定**

  * ゲーム開始時の所持ポイントを設定（デフォルト: `60`点）

* **端数処理ルールの設定**

  * 「全員から」点数を獲得する際、敗者の人数で割り切れない（端数が出る）場合の処理ルールを選択

  * **選択肢**:

    1. **ランダム**: 敗者の中からランダムで選ばれたプレイヤーが端数（余り）を多く負担する。

    2. **得点を減らす**: 端数を切り捨て、勝者の獲得点数を調整（割り切れる最大値）して均等に徴収する。

    3. **得点の多いプレイヤーから取る**: 現時点で持ち点が最も多い敗者が端数を多く負担する（最多得点者が複数いる場合は、その中からランダム）。

* **表示モード設定**

  * 通常表示とE-Ink（電子ペーパー・モノクロ端末向け高コントラスト表示）モードの切替

* **設定保持**

  * 設定した内容（人数、名前、初期点数、端数ルール、表示モード）は `localStorage` に自動保存される。

### 3.2 対局中機能（メイン画面）

* **スコア表示**

  * 各プレイヤーの現在の持ち点を大きくカード形式で表示

  * 持ち点がマイナス（0点未満）になってもゲームは継続（マイナス表示を維持）

* **点数移動入力**

  * **勝者選択**: 勝ったプレイヤーを1人選択

  * **獲得ポイント入力**: 獲得した点数を入力

  * **支払いタイプの選択**:

    * 「全員から」: 勝者以外の全員から点数を徴収

    * 「誰かから」: 特定の1人から点数を徴収

  * **ターゲット選択**: 「誰かから」を選択した場合、支払い対象となるプレイヤーを1人選択

  * **確定操作**: OK（確定）ボタンを押すことで点数を更新

* **Undo（取り消し）機能**

  * **直前1回分のみ**のスコア変動を取り消し、1局前の状態に戻すことが可能

* **ゲームリセット機能**

  * プレイヤー人数やニックネームの設定を維持したまま、全員の点数のみを「初期点数（デフォルト60点）」に戻して新しいゲームを開始する

## 4. 計算ロジック・詳細仕様

### 4.1 「誰かから」の点数移動

* 敗者の点数から「獲得ポイント」をマイナスし、勝者の点数に「獲得ポイント」を加算する。

### 4.2 「全員から」の点数移動および端数処理

* **基本計算**:
  

  $$
  \text{基本徴収額} = \lfloor \frac{\text{獲得ポイント}}{\text{敗者の人数}} \rfloor
  $$

  $$
  \text{端数（あまり）} = \text{獲得ポイント} \pmod{\text{敗者の人数}}
  $$

* **端数処理ルール別の挙動**（例: 3人プレイ、勝者15点勝ち $\rightarrow$ 敗者2人、基本徴収額7点、端数1点の場合）:

  1. **ランダム**

     * 敗者の中からランダムで1人を選出し、その人は `基本徴収額 + 端数`（8点）を支払う。

     * その他の敗者は `基本徴収額`（7点）を支払う。

     * 勝者は指定ど元の `獲得ポイント`（15点）を得る。

  2. **得点を減らす**

     * 端数を切り捨て、全員から `基本徴収額`（7点）を支払う。

     * 勝者の獲得ポイントは `基本徴収額 × 敗者の人数`（14点）に自動調整される。

  3. **得点の多いプレイヤーから取る**

     * 敗者の中で現時点の持ち点が最も多いプレイヤーが `基本徴収額 + 端数`（8点）を支払う。

     * 最多得点者が複数いる場合は、その対象者の中からランダムで1人選出する。

     * その他の敗者は `基本徴収額`（7点）を支払う。

     * 勝者は指定ど元の `獲得ポイント`（15点）を得る。

## 5. UI/UX 要件

* **画面レスポンシブ設計**

  * スマートフォンおよびタブレットをスタンドに置いて家族で囲む利用シーンを想定。

  * ボタン類はタップしやすいように大きめのサイズを確保。

  * スコアボードは一目で全員の状況が把握できる視認性の高いレイアウト（グリッド/フレックス）とする。

* **E-Ink（モノクロ端末）対応**

  * 電子ペーパー等のグラデーションや色の区別が困難な端末向けに、純黒白（ハイコントラスト）、太線・反転色を用いた「E-Inkモード」を提供。
  